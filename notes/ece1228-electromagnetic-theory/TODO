- do a final check of indexing at the very end.  Trying to do this incrementally as things are added.  done up to L5, ps4.

\frac{1}{16} r^2 r'^2 \lr{
 3 \left(4 \sin^2(t) \sin^2(\theta') \cos(2 p-2 \phi')
+4 \sin(2 t) \sin(2 \theta') \cos(p-\phi')
  +\cos(2 t) (3 \cos(2 \theta')+1)
  +\cos(2 \theta')\right)
  +1
}

\frac{1}{2} r^2 r'^2 \left(
\sin^2(t) \left(
   3 \sin(\theta') \left(
         \sin^2(p) \sin(\phi')+\cos^2(p) \cos(\phi')
      \right) (\sin(\theta') (\sin(\phi')+\cos(\phi'))+\cos(\theta'))
      -1
   \right)
+\cos^2(t) (
   3 \cos(\theta') (
      \sin(\theta') (\sin(\phi')+\cos(\phi'))
      +\cos(\theta')
   )
   -1
)
\right)

Q = \frac{1}{2} \left(x^2 \left(2 x'^2+3 x' (y'+z')-y'^2-z'^2\right)-x'^2 \left(y^2+z^2\right)+3 x' \left(y^2 y'+z^2 z'\right)+y'^2 \left(2 y^2-z^2\right)+3 y' z' \left(y^2+z^2\right)-z'^2 \left(y^2-2 z^2\right)\right)
