% consider switching to this package.  It typesets the | in a <a|b> much better when one of a,b is a big expression
% unfortunately, switching some of my previous stuff to use this is not so easy, since this uses a different 
% notation (it knows about the | character, whereas my \braket{}{} is a two argument beastie.

%\Requirepackage{braket}
%\newcommand{\setlr}[1]{\Set{#1}}
% this package also provides a \Set macro similar to my \setlr{}, but it also understands a | in the middle
% separating the set values from the constraints.

\newcommand{\bra}[1]{\left\langle {#1} \right\rvert}
\newcommand{\ket}[1]{\left\lvert {#1} \right\rangle}
\newcommand{\braket}[2]{\left\langle{#1} \middle\vert {#2}\right\rangle}

\newcommand{\ketbra}[2]{\ket{#1}\bra{#2}}

\newcommand{\BraOpKet}[3]{\bra{#1} \hat{#2} \ket{#3} }

% do not use this.  easier to just type \bra{} op \ket{} (less curly braces).
%\newcommand{\BraopKet}[3]{\bra{#1} #2 \ket{#3} }

