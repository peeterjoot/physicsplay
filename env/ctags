# 
# cd
# mkdir -p ~/.ctags.d
# cd ~/.ctags.d
# ln -s ~/physicsplay/env/ctags default.ctags

# C++ indexing configuration for vim
--c++-kinds=+p
--fields=+iaS
--extras=+q

# Exclude build artifacts
--exclude=build
--exclude=.git
--exclude=*.o
--exclude=*.so
--exclude=*.a

# Treat .hip files as C++ (for HIP/ROCm code)
#--langmap=C++:+.hip

# Recurse through directories
--recurse=yes
