#!/bin/bash

n=${1?}
#~/bin/ct basicStatMechProblemSet${n} basicStatMechProblemSetN tp$n
#perl -p -i -e "s/{N}/$n/g" basicStatMechProblemSet${n}.tex

for i in 2 ; do
   ~/bin/ct basicStatMechProblemSet${n}Problem${i} basicStatMechProblemSetNProblemM tp${n}$i
   perl -p -i -e "s/{N}/$n/g;s/{M}/$i/g;" basicStatMechProblemSet${n}Problem${i}.tex
done
