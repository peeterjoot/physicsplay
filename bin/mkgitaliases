
git config --global alias.ci 'commit -a'
git config --global alias.isci 'rm -n'

# I had the following to avoid typing 'origin master', but it appears from
# http://stackoverflow.com/questions/1741143/git-pull-origin-mybranch-leaves-local-mybranch-n-commits-ahead-of-origin-why
# http://stackoverflow.com/q/1741143
# that just pull/push can be used instead of these:
#git config --global alias.down 'pull origin master'
#git config --global alias.up 'push origin master'
