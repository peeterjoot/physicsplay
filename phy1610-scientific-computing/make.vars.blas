ifndef SCINET_OPENBLAS_INC
   ifdef USE_OPENBLAS
      # ubuntu libopenblas
      CXXFLAGS += -I/usr/include/openblas
      LDFLAGS += -L/usr/lib/openblas
      LDFLAGS += -lopenblas
   else
      # ubuntu libblas
      LDFLAGS += -lblas
   endif
endif
