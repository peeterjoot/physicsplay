#if 0
int myfuncEx( void ** ppBlkPtr, int line ) ;

#define myfunc( x ) myfuncEx( (x), __LINE__ )
//int myfunc( void ** ppBlkPtr ) ;
#endif

#include <stddef.h>
#define sqlzRc int
#define Uint32 unsigned
#define SQLO_MEM_POOL_HANDLE void *
   sqlzRc sqlogmblk
   (
      SQLO_MEM_POOL_HANDLE    poolHandle,
      const size_t            size,
      Uint32                  flags,
      void **                 ppBlkPtr
   ) ;

   sqlzRc sqlogblk
   (
      SQLO_MEM_POOL_HANDLE    poolHandle,
      const size_t            size,
      void **                 ppBlkPtr
   ) ;

void blah(void)
{
   int * r ;
   void * s ;

   sqlogmblk( 0, 1, 0, (void **)&r ) ;
   sqlogmblk( 0, 1, 0, &s ) ;

   sqlogblk( 0, 1, (void **)&r ) ;
   sqlogblk( 0, 1, &s ) ;
}
